// Django dev server (backend_django/), CORS-allowed for localhost:4200 (customer)
// and localhost:4300 (agent) — see supportflow/settings.py CORS_ALLOWED_ORIGINS.
export const API_BASE_URL = 'http://localhost:8000/api/v1';
